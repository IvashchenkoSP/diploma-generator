# Production settings
server.port=${PORT:8080}

# File handling
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# File storage
diploma.upload-dir=./uploads
diploma.output-dir=./output

# Logging
logging.level.com.diplomagenerator=INFO
